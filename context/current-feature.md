# Current Feature: Email Verification Toggle

<!-- Feature Name -->

## Status

<!-- Not Started|In Progress|Completed -->

In Progress

## Goals

<!-- Goals & requirements -->

- Add a single flag that turns the whole email-verification requirement on or off, so registration works for any address while Resend has no verified domain.
- One source of truth for the flag — a `src/lib/auth/verification-policy.ts` (or equivalent) module reading a server-only env var, `EMAIL_VERIFICATION_ENABLED`. No component or route reads `process.env` directly.
- Default to **enabled** when the var is unset, and treat only an explicit `"false"`/`"0"` as off, so a typo or a missing value leaves the current (secure) behaviour intact.
- With the flag **off**:
  - `POST /api/auth/register` creates the account, skips issuing a token, skips the send, and stamps `emailVerified` so the row is immediately sign-in-ready.
  - Credentials `authorize` in `src/auth.ts` skips the `EmailNotVerifiedError` gate.
  - `resendVerificationEmail` in `src/actions/auth.ts` refuses with a plain "verification is not required" rather than mailing anything.
  - `RegisterForm` skips the "check your email" panel and sends the user to `/sign-in` instead — driven by a field on the register response, **not** a `NEXT_PUBLIC_` var, so the flag stays server-only.
- With the flag **on**, every current behaviour is byte-for-byte unchanged: token issued, email sent, sign-in gated, resend form offered.
- `/api/auth/verify-email` and `/verify-email` keep working with the flag off, so a link mailed before the toggle flipped still redeems instead of 404ing or erroring.
- Document the var in `.env.example` alongside `RESEND_API_KEY`/`EMAIL_FROM`, saying plainly why you would turn it off and that it must be on in production.
- Verify both states against the dev server — register + sign in with a non-Resend address flag-off, and the full unverified-block flow flag-on — then `npm run build`, typecheck and lint.

## Notes

<!-- Any extra notes -->

**Why an env var over the alternatives.** A DB-backed setting or a config file would both be read from the same places an env var is, but neither is reachable from the credentials `authorize` path without another query, and the flag has to be decidable during `next build` and in CI where no database is guaranteed. A hardcoded constant would work but needs a redeploy to change and would sit in the diff of whoever forgets to flip it back.

**The decision that actually matters: what happens when the flag goes back on.** If a flag-off registration left `emailVerified` null, then turning verification back on would lock out every account created while it was off — they would need a resend round trip to get in, on an address nobody ever asked them to confirm. Stamping `emailVerified` at creation while the flag is off avoids that entirely and makes the flag purely about the *flow*, not about the state of existing rows. The tradeoff, worth stating out loud: those rows claim a verified address that was never proven, so they are indistinguishable later from ones that went through a real round trip. That is the correct reading of the policy ("we do not require proof right now") but it means flag-off accounts cannot be retroactively audited or re-challenged. The alternative — a nullable `emailVerified` plus a gate that skips the check — keeps the data honest but makes the toggle one-way in practice.

**Related decisions.**

- `EMAIL_FROM`'s doc comment and `src/lib/email.ts`'s header already explain the no-verified-domain limitation; the new var's comment should point at them rather than restate it.
- The sign-in page's resend form needs no gating of its own — it only renders when `SignInState.unverified` is true, which only the gate sets, so it goes quiet on its own once the gate is skipped.
- `prisma/seed.ts` already stamps `emailVerified` on both branches, so it needs no change either way.
- Leaving `sendVerificationEmail`'s dev-console link logging alone: it is inside the send path, which flag-off never reaches.
- No schema change and no migration — this is entirely a policy flag over columns that already exist.

**Out of scope.** The two known 500s recorded in the previous feature's history (`issueVerificationToken` inside the register route's outer try; `consumeVerificationToken` using `delete()` rather than `deleteMany()`) are untouched here — the flag makes the first one unreachable while off, which is not the same as fixing it.

## History

<!-- Keep this updated. Earliest to latest -->

- Project setup and boilerplate cleanup
- Initial Next.js + Tailwind CSS v4 setup: stripped create-next-app boilerplate (`page.tsx`, `globals.css`, default SVGs), committed as "chore: initial next.js and tailwind setup", added `origin` remote, and pushed `master` to GitHub
- Dashboard UI Phase 1: initialized shadcn/ui, added button/input components, set dark mode as default, built the `/dashboard` route with a top bar (logo, search, New Item button) and Sidebar/Main placeholders. Build passes.
- Dashboard UI Phase 2: built the collapsible sidebar — desktop icon-rail collapse and a mobile Sheet drawer, an item type list linking to `/items/TYPE` with counts, a Collections section with Favorites/Recent groups, a user avatar footer, and a "Navigation" header housing the sidebar toggle. Build passes.
- Dashboard UI Phase 3: built the main dashboard area — 4 stats cards (items, collections, favorite items, favorite collections), a Collections grid, a Pinned items section, and a Recent Items list, all sourced from mock data. Added shadcn Card/Badge components, widened the main content padding, and added a disabled "New Collection" button to the top bar. Build passes.
- Prisma 7 + Neon PostgreSQL: set up Prisma 7 (`prisma-client` generator → `src/generated/prisma`, `PrismaNeon` driver adapter, connection URLs in `prisma.config.ts`) against the Neon development branch. Created the initial schema (User, Item, ItemType, Collection, ItemCollection, Tag + NextAuth Account/Session/VerificationToken) with indexes and cascade deletes, applied it via `migrate dev --name init`, and seeded the 7 system item types. Added `src/lib/db-url.ts` to derive Neon's pooled/direct URLs from a single `DATABASE_URL`, a `src/lib/prisma.ts` hot-reload-safe singleton, and `scripts/test-db.ts` (`npm run db:test`) for connectivity checks. Build, lint, and db:test all pass.
- Seed Data: rewrote `prisma/seed.ts` to populate a demo dataset — the `demo@devstash.io` user (password bcrypt-hashed at 12 rounds), the 7 system item types, and 5 collections holding 18 items (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) with tags, real URLs for links, and a few pinned/favorite flags so the dashboard's pinned and favorites sections have real data. Added `bcryptjs`. The script is idempotent — system types are matched on `[name, userId: null]` and the demo user's collections/items are cleared and rebuilt on each run. Extended `scripts/test-db.ts` to fetch the seeded data back, assert the demo user, collection names and item counts, per-contentType content and collection membership, then print it all as a tree. Verified across two consecutive seed runs (5 collections / 18 items / 18 join rows, no duplicates); build, lint, and db:test all pass. Spec kept at `context/features/seed-spec.md`.
- Dashboard Collections: replaced the mock collection cards in the dashboard's main area with live Neon data. Added `src/lib/db/collections.ts` — `getRecentCollections()` (one query; item count via `_count`, type breakdown via a nested `items -> item -> itemType` join, so no N+1) and `getCollectionStats()` — plus `src/lib/db/user.ts` holding `getCurrentUserId()`, scoped to the seeded `demo@devstash.io` user until NextAuth lands. That helper is wrapped in React `cache()` so both fetchers share one lookup, and calls `connection()` from `next/server` before querying so Prisma results aren't baked in at build time (`export const dynamic` is on its way out in Next 16 once Cache Components is enabled); `/dashboard` now builds as `ƒ (Dynamic)`. Made `page.tsx` async and fetched both functions in a `Promise.all`; `CollectionsSection` now takes a `collections` prop instead of importing mock data. Card markup is unchanged apart from the accent colour, which is now the most-used *item type* in the collection (falling back to `defaultType`, then `--border`, for empty ones) rather than `defaultTypeId`, plus count pluralization and an empty state. Verified the rendered HTML against the seed — 5 cards with real cuid hrefs, correct counts and icon rows, and DevOps (2 links / 1 command / 1 snippet) correctly accented emerald. Build, typecheck, and lint all pass. Still mock and deliberately out of scope: the sidebar's collection lists (client component) and the Items / Favorite Items stat cards. Spec kept at `context/features/dashboard-collections-spec.md`.
- Dashboard Items: replaced the mock pinned and recent item rows in the dashboard's main area with live Neon data. Added `src/lib/db/items.ts` — `getPinnedItems()`, `getRecentItems(limit = 10)` and `getItemStats()`, all scoped through the existing `getCurrentUserId()` and returning empty/zero when there is no user. Both list queries share one `itemSummarySelect` (`satisfies Prisma.ItemSelect`, with a `Prisma.ItemGetPayload` row type feeding a single `toItemSummary` mapper), which pulls the item type (id/name/color) and tags in the same query so there is no N+1. `ItemRow` now takes an `ItemSummary` instead of a `MockItem` — its icon and left border come straight off `item.type` rather than a lookup into the mock type array, `createdAt` is a `Date`, and the description only renders when present since it is nullable. `PinnedItemsSection` and `RecentItemsSection` take an `items` prop; Pinned still returns `null` when empty and Recent gained an empty state matching the Collections section. `page.tsx` fetches the three item functions alongside the collection ones in one `Promise.all`, and the Items / Favorite Items stat cards are now real counts. Verified the rendered HTML against the seed — stats read 18 items / 5 collections / 3 favorite items / 2 favorite collections, and 13 rows (3 pinned above, 10 recent below) carry the correct type-derived icon and accent (orange terminal for the command, purple sparkles for the prompt, blue code for the snippet, emerald link for the links) with tag badges and pin/star markers. Build, typecheck, and lint all pass; `/dashboard` still builds as `ƒ (Dynamic)`. `src/lib/mock-data.ts` stays because the sidebar (a client component) still reads from it — deliberately out of scope. Spec kept at `context/features/dashboard-items-spec.md`.
- Stats & Sidebar: replaced the last of the dashboard's mock data — the sidebar — with live Neon data. Added `getItemTypesWithCounts()` to `src/lib/db/items.ts`: the shared system types plus any of the user's custom types (`where: { OR: [{ userId: null }, { userId }] }`), each with its item count as a *filtered* relation count (`_count: { select: { items: { where: { userId } } } }`) so it stays a single query and the globally-shared system types don't pick up other users' items. Item types have no ordering column, so display order is pinned in a `SYSTEM_TYPE_ORDER` constant with custom types sorting after them alphabetically; unlike the other fetchers this one still returns the system types when there is no user, because the sidebar's type list is navigation rather than data. Added `getSidebarCollections(limit = 5)` to `src/lib/db/collections.ts`, returning `{ favorites, recent }` as two queries so each group gets its own limit and favorites are excluded from Recent (`isFavorite: false`) — no collection appears twice. Extracted the card query's inline select and mapping into a shared `collectionSummarySelect` (`satisfies Prisma.CollectionSelect`, with a `Prisma.CollectionGetPayload` row type) and `toCollectionSummary`, mirroring the `itemSummarySelect` pattern, so `getRecentCollections` and the sidebar share one `summarizeTypes`/`accentColor` rule instead of duplicating it. `Sidebar` now takes `itemTypes` and `collections` props threaded through `SidebarNav` to `TypesSection`/`CollectionsSection`; it stays a client component (it consumes the `useSidebar` context), so `src/app/dashboard/layout.tsx` became async and fetches both in one `Promise.all`. Type rows keep their layout — `ITEM_TYPE_ICONS` icon tinted with the type's DB `color`, pluralized label, count, collapsed tooltip — and still link to `itemTypeRoute(name)`. Collection rows were factored into a `CollectionLink` wrapper: Favorites keep the yellow star, Recent swapped the item count for a `size-2.5` dot tinted by the collection's most-used item type (falling back to `--border`), and a "View all collections" link plus a "No collections yet" empty state were added below the groups. Verified the rendered HTML against the seed — all 7 types in spec order with correct colours and counts (4 snippets / 3 prompts / 5 commands / 0 notes / 0 files / 0 images / 6 links = 18 items), Favorites showing AI Workflows and React Patterns starred, and Recent showing Design Resources `#10b981`, Terminal Commands `#f97316`, DevOps `#10b981` — each dot matching the accent on that collection's main-area card. Build, typecheck, and lint all pass; `/dashboard` still builds as `ƒ (Dynamic)`. The stats cards needed no work — they were already real counts from the Dashboard Items feature, so the numbers above are verification only. `src/lib/mock-data.ts` survives on one import: the sidebar footer's `currentUser`, which goes when NextAuth lands. `/items/[typename]` and `/collections` still 404 — the spec only asked for the links. Spec kept at `context/features/stats-sidebar-spec.md`.
- Scrollbar Colors: the browser's default scrollbar track renders light, which showed up as a white strip down the right edge of the dashboard's main scroll area against the dark theme. Fixed with one declaration in `globals.css`'s base layer — `scrollbar-color: var(--muted) var(--background)` on `html`, so in dark mode the track matches the page background (`oklch(0.145 0 0)`) and the thumb is a subtle gray (`oklch(0.269 0 0)`). `scrollbar-color` is an inherited property and the theme tokens live on `html` (which also carries the `dark` class), so the main area, the sidebar nav and the mobile drawer all pick it up without per-component classes, and light mode resolves its own token values for free. Took the standard property over `::-webkit-scrollbar` pseudo-elements: it covers Chrome/Edge/Firefox, and Chrome ignores webkit scrollbar rules on an element once `scrollbar-color` is set there, so mixing both would be dead code. Scrollbar width left at the browser default — only the colors changed. Verified the rule in the CSS served by the dev server; build and lint pass.
- Sidebar Pro Badge: added a `PRO` badge to the Files and Images rows in the sidebar's TYPES list, marking the two Pro-only item types. Which types are Pro now lives in `src/lib/constants/item-types.ts` as a `PRO_ITEM_TYPES` set (`file`, `image`) behind an `isProItemType(name)` predicate, sitting alongside `ITEM_TYPE_ICONS`/`ITEM_TYPE_COLORS`/`itemTypeRoute` so all per-type metadata stays in one file — the schema has no `isPro` column and Pro-ness is a product decision about type *names*, so a constant is the right home until real gating lands. In `TypesSection` a small `ProBadge` wraps the shadcn `Badge` with `variant="outline"` and overrides the component defaults down to `h-4 px-1.5 text-[10px]` (from `h-5 px-2 text-xs`) plus `tracking-wide`, `uppercase` and `text-muted-foreground` — a hairline border and gray text, no fill competing with the type's coloured icon. The row's label was wrapped in a new inner `flex items-center gap-2` span holding label + badge, leaving the outer `gap-3` for icon-to-text spacing so the badge sits tighter to the label than the label does to the icon; `min-w-0` is repeated on the inner span so `truncate` still applies, and the badge inherits `shrink-0` from `badgeVariants` so a long custom type name truncates rather than crushing it. The item count stays at the far right. The collapsed icon rail gets a `· PRO` text suffix on its tooltip instead of the badge, because `TooltipContent` is inverted (`bg-foreground text-background`) and a badge styled for the sidebar background would read as a low-contrast smudge there. Verified against the served HTML — the badge renders on exactly the `files` and `images` rows and on none of the other five, counts unchanged (4/3/5/0/0/0/6), and `tailwind-merge` correctly dropped the `h-5 px-2 text-xs` defaults in favour of the overrides. The mobile drawer passes `collapsed={false}` so it gets the real badge with no extra handling. Two known limits, both left deliberately: `isProItemType` matches on name, so a future custom type named `file` would falsely earn a badge — the adjacent `ITEM_TYPE_ICONS[type.name]` lookup already behaves that way, and the real fix is an `isPro` column shipped with the actual gating; and this is a purely visual marker, so `/items/files` is still reachable (and still 404s like the other type routes) because Pro gating is deferred until launch per `context/project-overview.md`. Build, typecheck, and lint all pass. Spec kept at `context/features/add-pro-badge-sidebar.md`.
- Sidebar Collections Empty State: the sidebar's COLLECTIONS group rendered "No collections yet" *and* a "View all collections" link at the same time on an empty account — a link to a page with nothing on it, sitting directly under a message saying so. The two were independent siblings in `CollectionsSection` (`{isEmpty && <p>}` followed by an unconditional `<Link>`); they are now the two arms of one ternary on the already-computed `isEmpty`, which is what actually makes them mutually exclusive rather than merely usually-not-both. The gate is safe to hang the link off because `isEmpty` (`favorites.length === 0 && recent.length === 0`) means genuinely zero collections, not "none in the top 5" — `getSidebarCollections` partitions on `isFavorite` true/false with no other filter, so the two arrays union to every collection the user owns; had they overlapped or excluded anything, the link could have vanished while collections existed. The empty-state paragraph dropped from `text-sm` to `text-xs` to match the link, and picked up the link's `py-1.5` so the section doesn't change height when the two swap places — one class beyond the literal spec, flagged and kept. No collapsed-rail variant was needed: unlike `TypesSection`, `CollectionsSection` only renders under `{!collapsed && ...}`, and the mobile Sheet passes `collapsed={false}`, so both surfaces get the same behaviour from the one change. Verified against the dev server at both ends — seeded user gives one link and no paragraph; pointing `DEMO_USER_EMAIL` at a non-existent account (temporarily, then reverted — `src/lib/db/user.ts` is not in the branch diff) gives zero links and `<p class="px-2 py-1.5 text-xs text-muted-foreground">No collections yet</p>`. One UX tradeoff left deliberately: the link disappears exactly when a user might want it to create their first collection, so if `/collections` ever grows an empty-state CTA this is the line to revisit — `/collections` 404s today, so it changes nothing in practice. Build, typecheck, and lint all pass; `/dashboard` still builds as `ƒ (Dynamic)`. Total diff is 5 net lines in one file. Spec kept at `context/features/collections-section.md`.
- Audit Quick Wins: ran a full read-only audit of the codebase (0 critical, 0 high, 1 medium, 2 low) and shipped the one finding that survived triage — parallelizing `prisma/seed.ts`. The script made ~30 sequential round trips: 7 system-type upserts, then a nested loop creating each collection followed by its items one at a time. Now the system types resolve in a single `Promise.all` (each has a distinct name, so they cannot collide with one another), system types and the demo user seed concurrently, all 5 collections go in one round, and all 18 items go in one *flattened* round rather than a collection's worth at a time — roughly 4 sequential rounds in total, bounded below that only by the driver adapter's connection pool. Two hazards came with the parallelism, and neither was in the original spec. First, `connectOrCreate` on the items' tags would race on `Tag.name`'s unique constraint the moment two concurrent writes shared a tag — and eleven of the seed's items do — so tags are now created up front with one `createMany({ skipDuplicates: true })` and the items `connect` to them, removing the race rather than retrying through it. Second, parallel writes meant Postgres no longer stamped `createdAt`/`updatedAt`/`addedAt` in source order, so the dashboard's Recent Items list and the collection grid — both ordered by recency — shuffled between seed runs; a `seedTimestamp(index, total)` helper now spaces rows a minute apart by source index off a single `SEED_START` captured once per run, so write order stops mattering. That the explicit `updatedAt` actually lands was verified directly against the database rather than assumed: Prisma does honour it on a `@updatedAt` field, and `db:test` orders collections by *name*, so it would not have caught the value being silently ignored. Verified by seeding twice and diffing the full `db:test` tree — byte-identical across runs, every collection listing its items in exact source order, 5 collections / 18 items / 18 join rows / 29 tags, all 8 checks passing — and by rendering `/dashboard` on the dev server, which returns 200 with Recent Items in the same reverse-source order the sequential seed produced. Build, lint and typecheck all pass. A self-review caught two defects in the first pass, both fixed before commit: a comment claiming collections could not be deleted before their items, which is false since `ItemCollection` carries `onDelete: Cascade` on *both* relations (the delete order is a free choice, and the comment would have read as a constraint); and `Date.now()` being re-read per row, which made correctness rest on the `.map()` finishing inside the 60s spacing rather than on construction. Two audit findings were deliberately deferred: the collections type breakdown in `src/lib/db/collections.ts` still fetches one join row per membership just to compute three type icons and an accent colour, because a single-query fix needs `GROUP BY collection, item_type` — raw SQL was ruled out, Prisma's `groupBy` cannot group by a joined table's column (`ItemCollection` has no `itemTypeId`), and the alternatives (denormalizing that column onto the join table, or one `groupBy` per collection in a `Promise.all`) cost more than they buy until items CRUD exists to keep a denormalized column honest; and the dead exports in `src/lib/mock-data.ts` (`MockItemType`, `MockCollection`, `MockItem` and the `itemTypes`/`collections`/`items` arrays, none imported anywhere — `Sidebar.tsx` takes only `currentUser`) were left in place. One known cosmetic wart, flagged and left: collections and items are spaced off different totals (5 vs 18), so a collection's `createdAt` can be newer than the items inside it — nothing in the app compares the two. No schema change, so no migration. Also added `.claude/agents/code-auditor.md` in a separate commit — the read-only Sonnet agent that produced the audit, carrying the standing constraints that it must not report deferred features (auth, Stripe, R2, AI, Pro gating) as issues and must never report `.env` as exposed, since `.gitignore` covers `.env*`.
- Auth Setup (NextAuth + GitHub, phase 1 of 3): wired up NextAuth v5 (`next-auth@5.0.0-beta.32`) with `@auth/prisma-adapter@2.11.3` and GitHub OAuth, using the split config pattern. `src/auth.config.ts` holds the edge-safe half — `providers: [GitHub]` behind `satisfies NextAuthConfig`, nothing that touches the database — and `src/auth.ts` adds `PrismaAdapter(prisma)` plus `session: { strategy: "jwt" }` and exports `auth`/`handlers`/`signIn`/`signOut`. The spread order is `{ ...authConfig, adapter, session }` rather than the docs' `{ adapter, session, ...authConfig }` so a later addition to the shared config cannot silently clobber the adapter or the strategy — which matters in phase 2, when `auth.ts` is meant to *override* a Credentials provider declared in `auth.config.ts`. `src/proxy.ts` builds a second, adapter-free NextAuth instance from the same shared config and exports `export const proxy = auth(...)` with `matcher: ["/dashboard/:path*"]`; unauthenticated requests get a 307 to `/api/auth/signin` carrying `callbackUrl` built from `pathname + search`. Next 16 runs proxy on the Node runtime, so the split is no longer strictly an edge-compatibility requirement, but importing `@/auth` there would pull Prisma and the Neon driver into a bundle that executes ahead of every dashboard request, and the JWT strategy means the cookie alone answers "is this person signed in". Handlers are mounted at `src/app/api/auth/[...nextauth]/route.ts` as `export const { GET, POST } = handlers`. Per the spec no custom `pages.signIn` was set, so NextAuth's built-in page is what renders. One addition beyond the literal spec, flagged and kept: the spec asked to declare `Session.user.id` in `src/types/next-auth.d.ts`, but reading `@auth/core/lib/actions/session.js` showed the JWT branch builds `user` as exactly `{ name, email, picture }` — no `id` — so the declaration alone would have been a type that lies to every caller. `auth.ts` therefore carries a four-line `session` callback copying `token.sub` (which `@auth/core` sets to `user.id` at sign-in, per `lib/actions/callback/index.js`) onto `session.user.id`. That the augmentation is actually applied is proven by compilation itself: without it, `session.user.id = token.sub` would not typecheck. Verified against the dev server — `/dashboard` unauthenticated gives 307 to `/api/auth/signin?callbackUrl=%2Fdashboard`, `/dashboard/settings?foo=1` preserves the query in the callbackUrl, `/` and `/api/auth/*` stay 200 (outside the matcher), the sign-in page renders a "Sign in with GitHub" button with a hidden `callbackUrl=/dashboard` input, and POSTing to `/api/auth/signin/github` 302s to `github.com/login/oauth/authorize` with the right `client_id`, a `redirect_uri` of `/api/auth/callback/github` and PKCE `S256`. The authenticated branch was exercised by minting a real session JWT with the app's own `AUTH_SECRET` via `@auth/core/jwt`'s `encode` (salt = the cookie name `authjs.session-token`): with that cookie `/dashboard` returns 200 and `/api/auth/session` returns `{"user":{...,"id":"test-user-id-123"}}`, so the proxy's pass-through and the session callback are both confirmed rather than assumed. **The GitHub login leg itself is unverified** — completing it needs the user's own GitHub credentials, which were not requested; Playwright reached GitHub's real login page and stopped there. Because that left the adapter's write path untested, the riskiest silent failure was checked separately: `@auth/prisma-adapter` types its parameter as `PrismaClient` from `@prisma/client`, but this repo generates its client to `src/generated/prisma` and `@prisma/client`'s types resolve to `.prisma/client/default`, which does not exist — so under `skipLibCheck` the call compiles without the type ever being checked. A temporary read-only script called the adapter's own methods against Neon: `getUserByEmail("demo@devstash.io")` returned the seeded demo user, `getUser(id)` round-tripped it, and `getUserByAccount({ provider: "github", providerAccountId: ... })` returned `null` for a nonexistent account — that last call exercising the exact `accounts` compound-unique query path that sign-in uses. Both temp scripts were deleted. Documented `AUTH_SECRET` / `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET` in `.env.example` with the callback URL to register at GitHub; the real values were already present in `.env`. No schema change and no migration — the `User`/`Account`/`Session`/`VerificationToken` models have been in place since the initial Prisma migration, and the adapter's runtime field access matches them (its `authenticator` methods are WebAuthn-only and unused). Build, typecheck and lint all pass; the build now reports `ƒ Proxy (Middleware)` and `ƒ /api/auth/[...nextauth]`. Deliberately out of scope: `getCurrentUserId()` in `src/lib/db/user.ts` is still hardcoded to `demo@devstash.io`, so signing in with GitHub creates a new user row while the dashboard keeps rendering the demo user's 18 items — phase 3 owns the sidebar user, and repointing it now would leave a fresh account staring at an empty dashboard. Also noted and left alone: a stray unused `BETTER_AUTH_SECRET` in `.env`. Separately, `/.playwright-mcp/` was gitignored (a `chore:` commit) since the browser testing above litters it with page snapshots and console logs. Two things were deliberately *not* committed — the pre-existing unrelated `CLAUDE.md` Neon MCP section, and `.mcp.json`, which carries a live Context7 API key in plaintext and would have published a working secret had it been swept in with `git add -A`. Spec kept at `context/features/auth-phase-1-spec.md`.
- Auth Credentials (email/password, phase 2 of 3): added a Credentials provider alongside phase 1's GitHub OAuth, keeping the split config pattern intact. `src/auth.config.ts` declares it as an edge-safe placeholder — `Credentials({ credentials: credentialFields, authorize: () => null })` — so `src/proxy.ts`, which builds its own NextAuth instance from that same file, still bundles no Prisma and no bcrypt. `src/auth.ts` swaps that entry for the real one by mapping over the shared list (`typeof provider !== "function" && provider.type === "credentials"`) rather than redeclaring `providers` wholesale, so GitHub's entry — and anything added to the shared config later — carries through untouched. The real `authorize` parses with Zod, looks the user up by email, and `bcrypt.compare`s; it returns `null` on every failure path, which Auth.js turns into a generic `CredentialsSignin`, so a caller cannot tell "no such account" from "wrong password". `credentialFields` (the label/type/placeholder objects the built-in sign-in page renders) is exported from `auth.config.ts` and imported by `auth.ts` so the form and the validation can't drift. Phase 1's deliberate `{ ...authConfig, adapter, session }` spread order is what made the override land instead of being clobbered — the reason it was written that way. The registration endpoint is `src/app/api/auth/register/route.ts`: `POST` with name/email/password/confirmPassword, hashed at 12 rounds to match `prisma/seed.ts`, `select`ing only id/name/email back so the hash cannot reach the response. It returns 201/400/409/500 and catches Prisma `P2002` in addition to the up-front `findUnique`, because two concurrent submissions can both pass the existence check and the unique index on `users.email` is what actually decides. One file beyond the literal spec, flagged and kept: `src/lib/validations/auth.ts`, holding both schemas. It exists because email is trimmed and lowercased *before* validation (`z.string().trim().toLowerCase().pipe(z.email())`) and register and sign-in must agree on that rule — inline schemas in two files would let them drift, and the failure is silent: an account registered as `Test@x.com` that can never sign in. `zod@4.4.3` was a new dependency; it wasn't installed, and `context/coding-standards.md` mandates Zod for input validation. No schema change and no migration — `User.password` has been `String?` since the initial migration, confirmed by `prisma migrate status`. Verified against the dev server rather than assumed: register returns 201 with no hash in the body; duplicate/mismatch/bad-email/short-password/missing-name/non-JSON all return the right code and message; `  TEST@TEST.COM  ` 409s against the existing lowercase row, proving normalization; the **seeded demo user** (`demo@devstash.io` / `12345678`) signs in, which exercises `compare` against a hash the seed wrote rather than one the register route wrote; wrong password and unknown email produce byte-identical `CredentialsSignin` redirects; the real GitHub-only account in the database (`boacjeremiah@gmail.com`, `password IS NULL` — phase 1's "unverified GitHub leg" has since been completed) is rejected with no session cookie, exercising the null-password branch against a genuine row; a successful sign-in 302s to `/dashboard` and `/dashboard` then returns 200 with the right `user.id` in `/api/auth/session`; GitHub OAuth still 302s to `github.com/login/oauth/authorize` with client_id and PKCE; and the proxy still 307s signed-out `/dashboard` traffic. A `SELECT` against Neon confirmed the stored password is `$2b$12$…`, 60 chars. `/api/auth/register` correctly beats the `[...nextauth]` catch-all — static segments outrank catch-alls — confirmed by both the 201 and `ƒ /api/auth/register` in the build output. A self-review caught three things, all fixed before the commit: a `RegisterInput` type export that nothing imported, `MIN_PASSWORD_LENGTH`/`MAX_PASSWORD_LENGTH` exported speculatively when only their own module used them, and the generated-Prisma import sitting last in the register route's `@/` group where the existing convention (`src/lib/db/collections.ts:3`) puts it first. Four behaviours recorded as known and deliberate rather than defects: registering an email and later signing in with GitHub on that same address throws `OAuthAccountNotLinked`, which is Auth.js's safe default against takeover-by-email-claim and a phase-3+ decision if linking is ever wanted; registration openly confirms whether an email exists (the spec requires it), so no timing equalization was added to `authorize` — hardening one side while the other answers plainly would be theatre; there is no rate limiting on the public register endpoint, which needs a real decision before launch; and `emailVerified` stays null for password accounts, which nothing gates on today. One quirk worth knowing for phase 3: Auth.js's built-in credentials form emits no hidden `callbackUrl`, so the redirect target comes from the `authjs.callback-url` cookie — passing `callbackUrl` in the POST body is silently ignored despite `@auth/core/lib/index.js:18` appearing to read it, so the custom form should use `signIn("credentials", { redirectTo })`. Build, typecheck and lint all pass. Deliberately out of scope, as in phase 1: `getCurrentUserId()` in `src/lib/db/user.ts` is still hardcoded to `demo@devstash.io` — phase 3 owns wiring the session through to the dashboard. Two loose ends left for the user: a `test@test.com` row the spec's curl test created on the Neon development branch, not deleted without a go-ahead, and an unrelated pending edit to `.claude/skills/feature/SKILL.md` (reordering the `complete` workflow) that was folded into this commit at the user's request. Spec kept at `context/features/auth-phase-2-spec.md`.
- Auth UI (sign in, register & sign out, phase 3 of 3): replaced Auth.js's built-in pages with custom ones and made the dashboard render the real session user, closing out the auth work. A `(auth)` route group holds `layout.tsx` (centred card, DevStash wordmark linking home) plus `sign-in/page.tsx` and `register/page.tsx`; `src/components/auth/` holds `SignInForm`, `RegisterForm`, `FormError`, `PasswordInput`, `SubmitButton` and `RegisteredToast`; `src/components/shared/UserAvatar.tsx` and `src/actions/auth.ts` are new, as are the shadcn `dropdown-menu`, `label` and `sonner` primitives. `pages: { signIn: "/sign-in" }` went into `src/auth.config.ts` rather than `src/auth.ts` so the proxy's own NextAuth instance — built from that same shared file — agrees about where sign-in lives; that page therefore also receives Auth.js's off-page error redirects (`?error=OAuthAccountNotLinked` and friends), which `sign-in/page.tsx` maps through a `SIGN_IN_ERRORS` table to real sentences with a generic fallback. Sign-in runs through `signInWithCredentials`, a Server Action driven by `useActionState`, so a rejected attempt renders inline instead of bouncing through `/sign-in?error=`; the action seeds its state from the URL's error so a later submission replaces it rather than the two racing, and echoes the submitted email back because React resets an uncontrolled form once an action settles (the field carries `key={state.email}` so it remounts rather than mutating `defaultValue`, which Base UI warns about). `callbackUrl` is passed as `signIn("credentials", { redirectTo })` exactly as phase 2's note prescribed — the built-in form's hidden-input route is ignored by Auth.js — and every value is filtered through a `safeRedirect` that requires a single leading slash (`/^\/(?!\/|\\)/`), so `//evil.com` and `/\evil.com`, which are paths to a browser but origins to a redirect, fall back to `/dashboard`. GitHub sign-in is a sibling form rather than a nested one, since HTML forbids nesting. `RegisterForm` posts to `/api/auth/register` instead of going through a Server Action because the caller needs the status code to tell a duplicate email from a bad payload, and the route is the one a future CLI client would use; it runs the *same* `registerSchema` as the route handler so mismatched passwords and malformed emails are caught without a round trip while the server still trusts none of it, and `toFieldErrors` keeps only the first message per field since a stack of them under one input is noise. `getCurrentUserId()` in `src/lib/db/user.ts` now reads `session.user.id` off the JWT rather than looking up the hardcoded `demo@devstash.io`, which is the change that makes the entire dashboard session-scoped rather than just the footer; the `connection()` call it used to need is gone because reading the session cookie is itself a dynamic API, and `/dashboard` still builds `ƒ (Dynamic)`. `src/app/dashboard/layout.tsx` fetches `auth()` alongside the sidebar data in one `Promise.all` and redirects on a missing session — a type guard more than a branch, since the proxy already turns that traffic away, but the layout must not render a footer for nobody if that ever stops being true. `UserFooter` in `Sidebar.tsx` was rebuilt around a `DropdownMenu` whose trigger is the avatar plus name/email: the label degrades `name → email → "Your account"` because every field on a session user is optional (a GitHub account can arrive with no display name, and an OAuth profile with a private address without an email either), and the email line is skipped when it is already the label. The menu opens `side="top"` with a fixed `w-52` — anchor width would match the narrow collapsed rail — carries the name block as a `/profile` link, and puts Sign out in a `<form action={signOutAction}>` so signing out is a POST that no link prefetch can trigger on its own. `UserAvatar` uses the GitHub `image` when present and initials otherwise, via `AvatarImage`'s built-in fallback swap so a dead image URL degrades to initials rather than a blank circle; `getInitials` gives two letters for a multi-word name ("Brad Traversy" → "BT", first + last, so middle names are skipped), the first two characters of a single-word name (a lone "J" reads as a placeholder next to everyone else's two letters), one letter from an email's local part when there is no name, and a `User` icon when there is neither — with `Array.from` throughout so a name starting with an emoji yields that character instead of half a surrogate pair. Two things arrived mid-feature at the user's request. First, a success toast after registering: `sonner` was added via the shadcn CLI, which also pulled in `next-themes` — removed, because this app hardcodes `dark` on `<html>` with no theme provider, so `useTheme()` would have fallen back to `"system"` and shown a *light* toast to anyone whose OS is light; the component pins `theme="dark"` with a note to revisit when the optional light mode lands. `RegisteredToast` fires on mounting under `?registered=1` and then strips that param, reading `location` rather than `useSearchParams` so a `callbackUrl` carried through by the proxy survives the rewrite, and using a fixed toast id so React's development double-mount updates the existing toast instead of stacking a second. The inline "Account created" banner the page used to render was removed — two copies of one message in a single viewport reads as a bug. **A real defect surfaced here and is worth remembering: `<Toaster />` was first placed after `{children}` and the toast silently never appeared.** Sonner's store publishes only to *current* subscribers (`addToast` calls `publish` then appends) and both `Toaster` and `useSonner` initialize their list to `[]` without ever replaying `ToastState.toasts`, while React runs a descendant's mount effect *before* a later sibling's — so a toast raised in a page's mount effect on a full page load was published to nobody. `<Toaster />` now renders before `{children}`, with the ordering invariant commented in the root layout because it governs any mount-time toast, not just this one; it is `position: fixed`, so being first in the DOM costs nothing visually. Second, signed-in users are now bounced off the auth pages: `src/proxy.ts` guards both directions, its matcher extended to `["/dashboard/:path*", "/sign-in", "/register"]`. That guard lives in the proxy rather than the `(auth)` layout specifically to keep `/register` statically prerendered — an `auth()` call there would have forced both pages dynamic for a check the proxy already performs. Two details in it are load-bearing. The matcher uses inlined literals, not `...AUTH_ROUTES`: `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md:136` states matcher values must be constants for build-time static analysis and that "dynamic values such as variables will be ignored", so the spread that was written first would not have errored — it would have matched nothing and left the guard looking implemented while doing nothing. And the bounce is gated on `req.method === "GET"`, because a redirect preserves the method: bouncing a POST would send a form submission to `/dashboard`, which cannot answer it, reachable by leaving `/sign-in` open in a second tab and submitting it after signing in in the first; letting the POST through costs nothing since the action redirects to the dashboard itself. Verified against the dev server throughout rather than assumed. Signed out: `/dashboard` → `/sign-in?callbackUrl=%2Fdashboard`, both auth pages render their forms. Credentials sign-in as the seeded demo user lands on `/dashboard` showing 18 items / 5 collections / 3 favorite items / 2 favorite collections with the sidebar's real counts (4/3/5/0/0/0/6) and a `DU` avatar, which is the proof that `getCurrentUserId` is genuinely session-driven; a separate real account (`juan1@yopmail.com`) rendered `JC` over an empty dashboard, the outcome the phase notes predicted. `callbackUrl` round-trips (`/dashboard?foo=bar` lands there), while `https://evil.example.com`, `//evil.example.com`, `/\evil.example.com` and `javascript:alert(1)` all fall back to `/dashboard`. A wrong password gives an inline "Invalid email or password" with the email preserved and **no session cookie issued**. All four register-form validation cases (mismatch, bad email, short password, empty name) block submission with **zero POSTs fired** and `aria-invalid` set. The dropdown yields `/profile` plus a Sign out that lands on `/sign-in` with the session cookie gone, after which `/dashboard` is protected again — that sign-out leg was the real regression risk of the new guard, since a cookie cleared a beat too late would have bounced the user back to `/dashboard` and made signing out look broken; it doesn't, because the clearing `Set-Cookie` is applied before the browser follows the redirect. Signed in, `GET /sign-in` returns `307 Location: /dashboard` and `/register` the same, query strings included, while `POST /sign-in` returns `200` and passes through, confirming the method guard. Two edge cases closed by composition rather than by rule: `/api/auth/signin`, deliberately outside the matcher, 302s to `/sign-in` which the proxy then bounces, and `/sign-in/` normalizes before the matcher runs, so exact-match paths suffice. Build, typecheck and lint all pass; `/register` is still `○ (Static)` and `/sign-in` `ƒ`. `prisma/seed.ts` gained `SEED_USER_EMAIL` (documented in `.env.example`) so the sample content can be loaded into an account you actually sign in with, which is the practical answer to a fresh account seeing an empty dashboard; it refuses to invent a missing account and takes an existing one exactly as it is rather than rewriting its name or forcing the demo password onto an OAuth login. Beyond the goals and folded in at the user's instruction to commit everything: `devIndicators: false` in `next.config.ts`, unrelated to auth. Known and deliberate, none of them defects: `src/lib/mock-data.ts` is now dead code with zero imports repo-wide — the sidebar footer's `currentUser` was its last one — and was left in place because deleting files needs an explicit go-ahead, so it is the obvious next cleanup; `/profile` does not exist, so the dropdown's primary link 404s like `/collections` and `/items/[type]` before it (the goal asked only for the link); every `AuthError` collapses to "Invalid email or password", so a `Configuration` failure such as a missing `AUTH_SECRET` would misreport as a bad password; `/api/auth/register` has no session check, so a signed-in user can still POST it and create a second account — navigation is guarded, the endpoint is not, which is arguable given it exists for a future CLI client; `AUTH_ROUTES` and the proxy matcher must be kept in step by hand, and since the guard falls through to "protected", adding a *public* route to the matcher alone would silently start requiring a session for it; the signed-in bounce always targets `/dashboard` and ignores a stale `callbackUrl`, as asked, though `safeRedirect` already holds the validation logic if that should ever be honoured; and `dashboard/layout.tsx` decodes the JWT twice per request (its own `auth()` plus `getCurrentUserId()`'s), which a `getCurrentUser()` helper would fold. Still unverified after three phases: the GitHub OAuth login leg itself and `UserAvatar`'s *image* branch — every account tested falls back to initials, and completing the OAuth leg needs the user's own credentials. Loose ends on the Neon development branch, neither deleted without a go-ahead: the `test@test.com` row from phase 2 and the `juan1@yopmail.com` account created while testing this one. Spec kept at `context/features/auth-phase-3-spec.md`.
- Email Verification on Register: registering now sends a Resend email carrying a single-use link, and credentials sign-in is refused until that link is followed. **No schema change and no migration** — `User.emailVerified` and `VerificationToken` have both existed since the initial migration. `VerificationToken` is the Auth.js adapter's own table, shared with the Email/magic-link provider this app does not use, so it was reused under an `email-verification:<email>` identifier prefix; the prefix is checked on redemption, so an adapter-issued magic-link token can never be redeemed here and vice versa. `src/lib/auth/verification.ts` holds `issueVerificationToken` / `consumeVerificationToken` / `verificationCooldownRemaining` / `buildVerificationUrl`. Only a **SHA-256 hash** of the token is stored — unsalted deliberately, since the token is 256 bits of `randomBytes` so there is nothing to brute-force or rainbow-table and lookup has to stay one indexed query on an exact value — and the raw value exists only inside the emailed link. Issuing deletes any previous token for that address, so there is exactly one live link per account. The cooldown derives its issue time from `expires - TTL` rather than storing one, because reusing the adapter's table leaves no spare column. `src/lib/email.ts` builds the Resend client lazily (the key is absent during `next build`, and a missing key should degrade to "could not send" rather than crash the importing route) and `sendVerificationEmail` **never throws** — a failed send is a state to report, not a reason to fail the thing that triggered it. In development it also logs the link to the server console, which is the only way to complete the flow locally when mail cannot be delivered. Redemption is a **route handler**, `src/app/api/auth/verify-email/route.ts`, not the page: redeeming is a write, and a page that mutates while rendering is at the mercy of however many times the framework renders it. It consumes once and 303s to `/verify-email?status=…`, so the page is pure presentation, the token never reaches it, and it cannot leak through a referrer header or sit in browser history as a working credential. The page renders four outcomes — verified, already-verified, expired, invalid — and offers the resend form on the latter two. `invalid` is deliberately *not* worded as an error the visitor caused: a used token and one that never existed are indistinguishable after the fact, and mail scanners routinely fetch links before a human clicks. The sign-in gate lives in the credentials `authorize` **after** the `bcrypt.compare`, which is the whole point — rejecting unverified addresses up front would have been an account-enumeration oracle, so only someone who already knows the password learns anything. It is in the credentials path alone, never a shared callback, so **GitHub logins are untouched** (GitHub has already proven that address, and those rows carry no `emailVerified` of ours). Telling it apart from a wrong password needed `EmailNotVerifiedError`, a `CredentialsSignin` subclass in `src/lib/auth/errors.ts`; reading `@auth/core/lib/actions/callback/index.js:232` confirmed `provider.authorize` is called *outside* any try/catch and that Auth.js only substitutes its own generic `CredentialsSignin` when authorize *returns* null — a thrown error propagates untouched, which is what makes the distinction possible at all. It lives in its own module because `src/auth.ts` throws it and `src/actions/auth.ts` catches it, and importing either from the other would be a cycle. Registering no longer redirects to `/sign-in?registered=1`: signing in is blocked until the address is confirmed, so sending someone to a form they cannot use is a dead end. `RegisterForm` swaps itself for a "check your email" panel in place, and reports `emailSent` honestly rather than claiming success — the account is created even when the provider rejects the send, and saying "check your inbox" regardless would leave that person waiting on mail nobody accepted. `ResendVerificationForm` is shared by that panel, the verify page and the sign-in form; on sign-in it renders *above* the form rather than replacing it, so an unverified account is not a dead end for someone who simply meant to use a different account. Its address is always an editable field, never hidden, because the link is commonly opened on a different device from the one that registered. The resend action answers plainly whether an address has an account, deliberately matching the register endpoint which already replies "An account with that email already exists" — being coy here while the front door announces it would cost the honest user clarity and buy an attacker nothing. Its 60-second per-address cooldown is the throttle that matters, since this is otherwise an unauthenticated endpoint that will mail any address as often as asked. `prisma/seed.ts` now stamps `emailVerified` on the **update** branch as well as `create`: the demo row already existed unverified, and an update that skipped it would have left the seeded account unable to do the one thing it exists for. Verified against the dev server rather than assumed. Since only the hash is stored, a temporary script minted tokens to exercise every branch (deleted afterwards): a valid token gives `verified` and the account then signs in; the same token again gives `invalid`, so single-use holds; garbage and a missing `?token=` both give `invalid`; a token for an already-verified user gives `already-verified`; a past-dated token gives `expired`. The gate blocked an unverified account with the distinct message, no session cookie, and the resend form prefilled. All five resend branches answered correctly — unknown address, already verified, GitHub-only, real send, and cooldown ("try again in 57 seconds"). Signed-in users can still reach `/verify-email`, which is deliberately outside the proxy's auth-route bounce because a verification link must work whoever is signed in. **Real delivery was confirmed by the user's own round trip**, which also surfaced a defect my testing missed: `<Button render={<Link/>}>` needs `nativeButton={false}`, because Base UI assumes a real `<button>` and warns that the native semantics it relies on are gone. I had only ever checked those pages' *rendered output*, never the browser console, so a render-time error walked straight past; the re-test now captures console output and reports zero errors across all four statuses. Worth knowing for anyone testing this: the Resend account has **no verified domain**, so `onboarding@resend.dev` delivers *only* to the account owner's own address (`boacjeremiah@gmail.com`) — a direct API probe returned `403 validation_error` saying exactly that, and the fact it is a *validation* error rather than a `401` is what proves the API key itself authenticates fine. Everything else gets `emailSent: false` and the "could not send" panel, which is why that branch is the one exercised locally. **Two defects were found in review and shipped unfixed at the user's instruction, both reachable 500s:** `src/app/api/auth/register/route.ts` runs `issueVerificationToken` inside the outer try, so a failure there returns 500 "Could not create the account" on an account that *was* created, leaving the caller to retry into a permanent 409 — and the comment directly above it claims this exact thing must not happen, so the code does not deliver its own stated intent; and `consumeVerificationToken` uses `delete()` rather than `deleteMany()`, so two redemptions that both pass the `findUnique` before either deletes will have the loser throw `P2025` unhandled, 500ing instead of rendering the invalid state — reachable by the scanner-prefetch-plus-human-click case the surrounding code already anticipates. A `$transaction` around the delete and the `user.update` would close both, plus a third lower-severity gap: the token is deleted before the update, so a failed update spends the link without verifying the address. Three smaller notes also left: the cooldown starts when the token is issued rather than when the send succeeds, so a failed send still costs 60 seconds; `emailSent: result?.data?.emailSent !== false` treats a *missing* field as success when absent should mean unknown; and `SIGN_IN_ERRORS` does not map `CredentialsSignin`, so an `email_not_verified` arriving by URL redirect rather than through the server action would show the generic message. Beyond the goals and folded in at the user's request: `scripts/prune-users.ts` (`npm run db:prune`), which deletes every user except a keep-list and is **dry-run by default**, requiring `--yes` to write — it handles the two things that do not cascade (`VerificationToken` has no user relation, `Tag` is global) and refuses to run if the keep-list is empty or names an address that does not exist, since a typo there is unrecoverable; and the removal of the register card's `CardTitle`/`CardDescription`. Running that script deleted the user's own GitHub account along with 18 items and 5 collections — flagged from the dry run beforehand and confirmed, but worth recording that the default keep-list is demo-only and `KEEP_USER_EMAILS` is how to spare a real login. `src/components/auth/RegisteredToast.tsx` is now fully dead — nothing imports it, and it was the only `toast()` caller in the app, so `<Toaster />` in the root layout has no producers either; both were left in place because deleting files needs an explicit go-ahead, and the Toaster is worth keeping for future CRUD toasts. Build, typecheck and lint all pass; `/register` is still `○ (Static)` and the build now reports `ƒ /api/auth/verify-email` and `ƒ /verify-email`.
- Sign Out a Deleted User: deleting a user row left that person signed in — reported from the browser, where reloading `/dashboard` after deleting the account kept rendering it. Sessions are JWTs (`session: { strategy: "jwt" }` since auth phase 1), so the signed cookie *is* the session: `src/proxy.ts` decides `signedIn` from the cookie's signature alone, `getCurrentUserId()` reads `token.sub`, and `src/app/dashboard/layout.tsx` only redirected on a *missing* session. Nothing in the app asked the database whether the account still existed, so a deleted user kept a working cookie for its full 30-day life, rendering the token's name and email over a dashboard whose every query came back empty. **No schema change and no migration.** Added `getCurrentUser()` to `src/lib/db/user.ts` — the session id resolved to a real `users` row (`id`/`name`/`email`/`image`), `cache()`d per request and built on `getCurrentUserId()` rather than calling `auth()` again, which also folds the double JWT decode flagged at the end of the Auth UI phase: one decode per request instead of one in the layout and one in the fetchers. Its `null` means something different from every other fetcher in `src/lib/db/` — "sign this session out", not "no data" — and the doc comment says so, because the whole point is that no other caller is watching for it. The layout now fetches it in place of `auth()` inside the existing `Promise.all` and redirects when the row is gone; it also feeds the sidebar footer, so the footer shows the account's current name, email and avatar rather than whatever they were when the token was minted. Clearing the cookie could not happen there: `signOut` writes the expiring cookies through `next/headers`, which a Server Component render cannot do, so the layout can *detect* the stale session but only a route handler can *fix* it — hence `GET /api/auth/stale-session`, which the layout redirects to. Three branches, in order: the account still exists → back to `/dashboard`; no session at all → plain `/sign-in`, because "you were signed out" would be a lie; otherwise `signOut({ redirectTo: "/sign-in?error=SessionInvalid" })`. The first is what makes a `GET` safe — a cross-site `<img src>` pointed at it clears nothing for someone whose account is fine, so it cannot be used to sign other people out — and `SessionInvalid` is a key of our own in `sign-in/page.tsx`'s `SIGN_IN_ERRORS`, which otherwise holds only Auth.js's codes. The **proxy was deliberately left alone**: it still bundles no Prisma and still decides only whether the cookie is signed, so the existence check costs one indexed lookup where the dashboard renders instead of a Neon round trip ahead of every matched request — the alternative was considered and rejected, since Next 16's Node-runtime proxy makes it possible but it reverses phase 1's split-config rationale. One consequence worth knowing: the *first* leg of a stale session is a pass-through, so the fix is a two-redirect chain rather than an immediate bounce. Verified against the dev server with a session cookie minted by `@auth/core/jwt`'s `encode` under the app's own `AUTH_SECRET` (salt = `authjs.session-token`) naming a user id that does not exist — the same state as a deleted row, without deleting one; the temporary script was removed afterwards. `/dashboard` → 307 `/api/auth/stale-session` → 307 `/sign-in?error=SessionInvalid`, 200, **session cookie gone from the cookie jar**, message rendered, two redirects and no loop — the loop being the real hazard here, since the proxy bounces signed-in traffic off `/sign-in` and would have ping-ponged had the cookie survived the hop. A valid session still renders `/dashboard` 200 with `Demo User` from the database and **zero** occurrences of the different name baked into that token, which is the proof the footer reads the row rather than the JWT; stats still 18 items / 5 collections / 3 favorite items / 2 favorite collections. A forced `GET` on the handler with that valid session 307s to `/dashboard` and sets **no cookies at all**. Signed out, the handler gives a plain `/sign-in`, and `/dashboard` still 307s to `/sign-in?callbackUrl=%2Fdashboard`, so the proxy path is unchanged. A real browser sign-in as the demo user landed on `/dashboard` with **0 console errors or warnings** — checked deliberately, since the previous feature's `nativeButton` defect walked straight past HTML-only verification. Build, typecheck and lint all pass; `ƒ /api/auth/stale-session` is in the build output and `/register` is still `○ (Static)`. Known and deliberate, none of them defects: the check runs only where the dashboard renders, so a deleted user's cookie stays technically valid for other endpoints (`POST /api/auth/register`, say) until they touch the dashboard; `getCurrentUserId()` stays JWT-only, so the data fetchers still scope on an unverified id, which is harmless because a nonexistent id simply yields empty results and the layout redirect wins; the layout's sidebar queries still run before the redirect, wasted work in the rare case, and serializing them would tax every healthy request to save the broken one; and `CurrentUser` restates the `select` shape by hand, which TypeScript keeps honest. Also run at the user's request, separately from the fix: `npm run db:prune` — the dry run first, which showed one doomed account (`boacjeremiah@gmail.com`, 0 items / 0 collections / 0 linked accounts, so the password registration rather than the GitHub login the earlier prune took), then `-- --yes` after confirmation, leaving only `demo@devstash.io`, 0 stale verification tokens, 0 orphaned tags, and the 7 system item types intact. That address is also the only one Resend can deliver to while the account has no verified domain, so testing the verification email again means re-registering it.
